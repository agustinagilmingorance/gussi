function mostrar()
{
	var letra; 
	var numero=parseInt(prompt("ingrese un numero entre -100 y 100"));
	var contador=0;
  	var acumulador=0;
  	var letraMin;
  	var numMin;
  	var numNeg;
  	var numPos;

  	for (contador=0; ;contador++)
  	{
  		while(contador=<-100||contador>=100)
  		{
  			alert ("ingrese un numero valido ");
  			numero=parseInt(prompt("reingrese un numero entre -100 y 100"))
  		}
  	}
